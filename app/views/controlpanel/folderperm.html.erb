<% form_tag(:controller => "controlpanel", :action => "updatefolderperm") do |f| %>
    <%= stylesheet_link_tag "folderperm" %>
    <h4>Set your folder permission for <b><%= @selectedfolder %></b>.</h4>
    <!-- Your file list - start -->
    <ul class="downloads">
        <li>
            <div class="LeftBox">
                <h4>Available Users</h4>
                <select multiple="true" class="ListOfUsers" name="listofusers">
                    <% @availableusers.each do |user| %>
                        <option title="<%= user.name %>" ><%= user.name %> </option>
                    <% end %>
                </select>
            </div>
            <div class="MiddleBox">
                <div class="cmdbutton"><a href="#">>>|</a></div>
                <div class="cmdbutton"><a href="#">&nbsp;>></a></div>
                <div class="cmdbutton"><a href="#"><<&nbsp;</a></div>
                <div class="cmdbutton"><a href="#">|<<</a></div>
            </div>
            <div class="RightBox">
                <h4>Selected Users</h4>
                <select multiple="true" class="ListOfUsers" name="selectedusers">
                    <% @selectedusers.each do |user| %>
                        <option title="<%= user.name %>" ><%= user.name %> </option>
                    <% end %>
                </select>
            </div>
            <div class="clearfix"></div>
        </li>
        <li>
            <a href="#" onclick="document.forms[0].submit()" class="btnupdatepermissions"> Update Permissions </a>
        </li>
    </ul>
<% end %>